import 'package:flutter/material.dart';

class Dimens {
  Dimens._();

  static const defaultSpacing = EdgeInsets.all(16);

  static const buttonSpacing = EdgeInsets.symmetric(
    horizontal: 20,
    vertical: 11,
  );
}
